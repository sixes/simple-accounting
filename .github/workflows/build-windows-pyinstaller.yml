name: Build Windows Executable (PyInstaller)

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        pip install PySide6
        pip install pyinstaller
        
    - name: Build with PyInstaller
      run: |
        pyinstaller --onefile --windowed --name=bankNote --hidden-import=excel_like --hidden-import=excel_table --hidden-import=sheet_manager --hidden-import=file_manager --hidden-import=dialogs main.py
        
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: bankNote-windows-pyinstaller
        path: dist/
